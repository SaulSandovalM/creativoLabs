import 'package:flutter/material.dart';

class MainServicesCat extends StatelessWidget {
  const MainServicesCat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('data');
  }
}
